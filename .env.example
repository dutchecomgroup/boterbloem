# --- Database ---
# Connect directly to the live VPS Postgres for both local dev and production.
# Local dev: use the public IP. Production (on VPS): use localhost.
DATABASE_URL=postgresql://abb_app:CHANGE_ME@85.215.182.227:5432/atelierboterbloem

# --- Server ---
PORT=6778
NODE_ENV=development

# --- Session ---
# Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
SESSION_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_STRING

# --- Uploads ---
UPLOADS_DIR=./uploads
MAX_UPLOAD_MB=10

# --- Site ---
PUBLIC_BASE_URL=http://localhost:6778
